# Task Management Validation Package

This package is used for shared validation across both the frontend and backend of the Task Management application using the `zod` library.

## Tech Stack

- **Zod** - Schema validation for TypeScript

## Features

- Shared validation logic between the API and the web app
- Ensures consistency and data integrity when handling task-related operations
